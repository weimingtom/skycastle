# Default boot configuration for Skycastle Server

# NOTE: Assumes the SGS / Darkstar install directory is inside the skycastle game directory.

# This seems to be relatively to the current directory from the caller.
SGS_DEPLOY=deploy

# This path is relative to the SGS homedir
SGS_PROPERTIES=../conf/SkycastleServer.properties

# This path is probably relative to the SGS homedir?
SGS_LOGGING=../conf/logging.properties

